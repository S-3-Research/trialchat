// Shared request/response schemas and domain types used by both apps/web and
// apps/agent. Keep this package free of framework-specific (Next.js /
// LangGraph) implementation details — only plain types/interfaces belong here.

export {};
