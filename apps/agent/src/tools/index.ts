// Placeholder for agent tools (e.g. clinical trial search, resource lookup).
// Mirror the logic currently living in apps/web/app/api/tools when it is
// migrated over — keep tool implementations agent-specific here rather than
// in packages/shared-types, unless the frontend also needs to call them
// directly.

export const tools = [];
